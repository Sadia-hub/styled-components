import { createGlobalStyle } from "styled-components";

export const GlobalStyles = createGlobalStyle`
    body{
    height:100vh;
    display:grid;
    place-items:center;
    background-color:cyan
    }
`

//you can access theme provider here too